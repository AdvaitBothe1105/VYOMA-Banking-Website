import { ReactNode } from "react";

export const metadata = {
  title: "Fund Transfer | Vyoma Bank",
  description: "Transfer funds securely and instantly with Vyoma Bank's digital platform.",
  keywords: ["Fund Transfer", "Vyoma Bank", "Send Money", "Banking"],
};

export default function FundLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}